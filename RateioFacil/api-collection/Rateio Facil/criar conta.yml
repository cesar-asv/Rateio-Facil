info:
  name: criar conta
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/contas
  body:
    type: json
    data: |
      {
        "descricao": "churras da galera",
        "donoId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
